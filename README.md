# Read config.ini and Convert into Parquet Format and Display Result

virtualenv .env
Windows:   
    .env\Scripts\activate.bat
Linux:
    source .env/bin/activate

pip install -r requirements.txt

python -m pip install --upgrade pip

python app.py